{
  "name": "wolfapp",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://www.tyleryep.com/wolfapp",
  "dependencies": {
    "gh-pages": "^3.1.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-redux": "^6.0.1",
    "react-scripts": "2.1.1",
    "redux": "^4.0.5",
    "redux-starter-kit": "^0.3.3"
  },
  "scripts": {
    "start": "NODE_PATH=src/ react-scripts start",
    "build": "NODE_PATH=src/ react-scripts build",
    "test": "NODE_PATH=src/ react-scripts test",
    "eject": "NODE_PATH=src/ react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
